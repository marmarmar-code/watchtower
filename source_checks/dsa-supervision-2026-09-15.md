# Platform supervision histories

A successful anonymous download of the official DSA overview contained 29 services and 145 dated actions. Replaying that one real response through two adapter polls produced zero alerts and identical state. This verifies the parser against observed data; it does not establish independent live repeatability. The 51 targeted synthetic/shared tests passed.

Both subsequent live adapter attempts ended with HTTP 504 before a successful snapshot. Two diagnostic clients then received the same HTTP 503 page stating “Backend fetch failed”. The feature is implemented but has not passed live adapter verification or isolated runtime initialization. No operational success is claimed. The adjacent JSON records response hashes and proof boundaries.

The parser preserves provider groupings, service classification, coordinator and dated action descriptions with official links. Requests, preliminary findings, commitments and decisions retain their source wording. User metrics are excluded. A source service name supplies identity, so renaming can appear as a new service. Absence never establishes termination or case closure. The overview may omit or lag events; linked documents are not downloaded.
