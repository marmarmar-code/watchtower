# Platform supervision histories

After a temporary source outage, the final adapter completed two successful live polls of the official DSA overview. Four complete anonymous responses produced 29 services, 145 dated actions, zero alerts and identical full state. The adjacent JSON includes response/code hashes and the earlier HTTP 503/504 failure boundary. Recovery was observed; continuous availability is not claimed.

The parser preserves provider groupings, service classification, coordinator and dated action descriptions with official links. Five continuation blocks inherit the preceding explicit provider. Requests, preliminary findings, commitments and decisions retain their source wording. User metrics are excluded. A source service name supplies identity, so renaming can appear as a new service. Absence never establishes termination or case closure. The overview may omit or lag events; linked documents are not downloaded.

The combined targeted checks passed 63 tests, covering this feature, certificate auctions and shared source contracts. Synthetic tests are distinct from the real-source proof. Runtime activation is recorded separately.
