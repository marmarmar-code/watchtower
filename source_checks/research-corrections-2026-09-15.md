# Research publication updates

The [official Crossref dataset documentation](https://www.crossref.org/documentation/retrieve-metadata/retraction-watch/) distinguishes retractions, corrections, expressions of concern and reinstatements. The latter categories have less complete coverage. The adapter preserves the four source labels separately and follows records selected by exact country tokens in the source affiliation field. Country and source reason labels do not establish author responsibility or misconduct.

Two actual final adapter polls each read the generation metadata and complete CSV twice. The export generated on 14 September 2026 contained 72,326 unique Record IDs and 199 wholly blank trailing rows. The selected country token matched 191 records: 167 retractions, 13 corrections, eight expressions of concern and three reinstatements. Both polls were quiet and produced identical full state. The adjacent JSON records response hashes and final code/configuration fingerprints. Fifty targeted synthetic contract and integration tests passed separately from the real-data check.

Record ID is the identity; several notices may concern the same original publication. Source dates remain date-only and do not become publication timestamps. The CSV has 21 exact columns including an unnamed empty final column. Only wholly blank trailing rows are ignored; a populated row after the trailer, duplicate ID, truncated response, stale or regressing generation date, or mismatch between complete reads preserves prior state. Whole-export reads validate identity and country before selection. Author order and multiplicity and literal institution text are retained. No relationship between their positions is inferred.

Blank and unavailable DOI values and blank or zero PubMed identifiers remain absent. Percent-encoded DOI characters are decoded once and safely encoded in the resolver URL; a semicolon inside a DOI remains part of the identifier. Link priority is notice DOI, notice PubMed, original DOI, original PubMed, then the dataset with Record ID in the alert. The actual selection yielded 182 notice DOI links, five notice PubMed links, three original DOI links and one dataset fallback. Original links and dataset fallbacks are labelled. The existing source URL field is preserved as text and is not followed by the adapter.

Initial observation is quiet. New records and changed fields may alert once; disappearance is not evidence of reinstatement. No paper text or independent investigation is assessed. This bounded bulk source downloads substantially more data than an ordinary page. Matching reads do not establish a transactional snapshot. Later scheduled operation and actual delivery remain unverified by these local checks.
